diff --git a/.github/workflows/code-review.yml b/.github/workflows/code-review.yml
index 3592d5a..98a1f9f 100644
--- a/.github/workflows/code-review.yml
+++ b/.github/workflows/code-review.yml
@@ -15,13 +15,13 @@ jobs:
     if: github.event.pull_request.draft == false
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
           ref: ${{ github.event.pull_request.head.sha }}
 
       - name: Install uv
-        uses: astral-sh/setup-uv@v4
+        uses: astral-sh/setup-uv@38f3f104447c67c051c4a08e39b64a148898af3a
         with:
           enable-cache: true
 
@@ -33,6 +33,7 @@ jobs:
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
@@ -42,6 +43,7 @@ jobs:
           git config user.email "cursoragent@cursor.com"
 
       - name: Perform automated code review
+        if: ${{ !github.event.pull_request.head.repo.fork }}
         env:
           CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
           MODEL: gpt-5
diff --git a/.github/workflows/fix-ci.yml b/.github/workflows/fix-ci.yml
index 93ffafe..a2ff060 100644
--- a/.github/workflows/fix-ci.yml
+++ b/.github/workflows/fix-ci.yml
@@ -13,16 +13,17 @@ permissions:
 jobs:
   attempt-fix:
     if: >-
-      ${{ github.event.workflow_run.conclusion == 'failure' && github.event.workflow_run.name != 'Fix CI Failures' }}
+      ${{ github.event.workflow_run.conclusion == 'failure' && github.event.workflow_run.name != 'Fix CI Failures' && github.event.workflow_run.head_repository.fork == false }}
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
diff --git a/.github/workflows/fix-conflicts.yml b/.github/workflows/fix-conflicts.yml
index 9883916..c459540 100644
--- a/.github/workflows/fix-conflicts.yml
+++ b/.github/workflows/fix-conflicts.yml
@@ -10,17 +10,18 @@ permissions:
 
 jobs:
   rebase:
-    if: ${{ github.event.action != 'labeled' || github.event.label.name == 'needs-rebase' }}
+    if: ${{ (github.event.action != 'labeled' || github.event.label.name == 'needs-rebase') && github.event.pull_request.head.repo.fork == false }}
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
           ref: ${{ github.event.pull_request.head.sha }}
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
diff --git a/.github/workflows/improve-pr-description.yml b/.github/workflows/improve-pr-description.yml
index 8063761..e230ae1 100644
--- a/.github/workflows/improve-pr-description.yml
+++ b/.github/workflows/improve-pr-description.yml
@@ -12,13 +12,14 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
           ref: ${{ github.event.pull_request.head.sha || github.sha }}
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
@@ -28,6 +29,7 @@ jobs:
           git config user.email "cursoragent@cursor.com"
 
       - name: Update PR body
+        if: ${{ !github.event.pull_request.head.repo.fork }}
         env:
           CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
           MODEL: gpt-5
diff --git a/.github/workflows/secrets-audit.yml b/.github/workflows/secrets-audit.yml
index bc31cb4..a4dee3a 100644
--- a/.github/workflows/secrets-audit.yml
+++ b/.github/workflows/secrets-audit.yml
@@ -15,12 +15,13 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
diff --git a/.github/workflows/test.yml b/.github/workflows/test.yml
index dbf7191..0d5e7c9 100644
--- a/.github/workflows/test.yml
+++ b/.github/workflows/test.yml
@@ -6,15 +6,18 @@ on:
   pull_request:
     branches: [main]
 
+permissions:
+  contents: read
+
 jobs:
   test:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
 
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
         with:
           python-version: "3.12"
 
diff --git a/.github/workflows/translate-keys.yml b/.github/workflows/translate-keys.yml
index cb785f8..2de5c73 100644
--- a/.github/workflows/translate-keys.yml
+++ b/.github/workflows/translate-keys.yml
@@ -14,13 +14,14 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
           ref: ${{ github.event.pull_request.head.sha }}
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
@@ -30,6 +31,7 @@ jobs:
           git config user.email "cursoragent@cursor.com"
 
       - name: Propose i18n updates
+        if: ${{ !github.event.pull_request.head.repo.fork }}
         env:
           CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
           MODEL: gpt-5
diff --git a/.github/workflows/update-docs.yml b/.github/workflows/update-docs.yml
index 9d2cf63..4866778 100644
--- a/.github/workflows/update-docs.yml
+++ b/.github/workflows/update-docs.yml
@@ -14,13 +14,14 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
           ref: ${{ github.event.pull_request.head.sha }}
 
       - name: Install Cursor CLI
         run: |
+          set -euo pipefail
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
@@ -30,6 +31,7 @@ jobs:
           git config user.email "cursoragent@cursor.com"
 
       - name: Update docs
+        if: ${{ !github.event.pull_request.head.repo.fork }}
         env:
           MODEL: gpt-5
           CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
diff --git a/.github/workflows/visual-testing.yml b/.github/workflows/visual-testing.yml
index 3953138..4c80fa3 100644
--- a/.github/workflows/visual-testing.yml
+++ b/.github/workflows/visual-testing.yml
@@ -15,13 +15,13 @@ jobs:
     if: github.event.pull_request.draft == false
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
         with:
           fetch-depth: 0
           ref: ${{ github.event.pull_request.head.sha }}
 
       - name: Install uv
-        uses: astral-sh/setup-uv@v4
+        uses: astral-sh/setup-uv@38f3f104447c67c051c4a08e39b64a148898af3a
         with:
           enable-cache: true
 
@@ -36,12 +36,10 @@ jobs:
           curl https://cursor.com/install -fsS | bash
           echo "$HOME/.cursor/bin" >> $GITHUB_PATH
 
-      - name: Install Chrome for Playwright MCP
-        run: |
-          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
-          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
-          sudo apt-get update
-          sudo apt-get install -y google-chrome-stable
+      - name: Setup Chrome
+        uses: browser-actions/setup-chrome@c785b87e244131f27c9f19c1a33e2ead956ab7ce
+        with:
+          chrome-version: stable
 
       - name: Configure git identity
         run: |
@@ -78,6 +76,7 @@ jobs:
         env:
           CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+        if: ${{ !github.event.pull_request.head.repo.fork }}
         run: |
           cursor-agent -p 'You are in a GitHub Actions runner with Playwright MCP tools. Perform lean visual testing and post one concise PR comment.
 
@@ -108,7 +107,7 @@ jobs:
           pkill -f "python -m cursor_cli_examples.app" || true
 
       - name: Upload screenshots as artifacts
-        uses: actions/upload-artifact@v4
+        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
         if: always()
         with:
           name: visual-testing-screenshots-${{ github.event.pull_request.number }}
